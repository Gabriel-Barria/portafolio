version: '3.8'

services:
  dev:
    build:
      context: .
      target: builder
    ports:
      - "3001:3000"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev

  prod:
    build:
      context: .
    ports:
      - "8080:80"
